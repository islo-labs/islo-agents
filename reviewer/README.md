# Reviewer Agents

Reviewer templates inspect external changes and post review feedback through the integration that owns the change.

Available reviewers:

- `github/`: reviews GitHub pull requests with an Islo job and the shared `src/agent.ts` runner.
