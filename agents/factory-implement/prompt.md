# Factory implement

Own one issue as a complete cross-repository feature. Fetch live Linear and
GitHub state, implement and test every required change, and create or update
the full pull-request set. Treat review and verification feedback across all
PRs as one feedback loop. Report ready only when the complete feature is ready
for review; otherwise report blocked with the existing complete PR set.
