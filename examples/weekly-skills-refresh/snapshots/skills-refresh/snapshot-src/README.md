# skills-refresh snapshot source

No files to copy from this directory. Bake product git checkouts under `/workspace/` on the build VM.

Agent behavior is defined in `agents/weekly-skills-refresh/prompt.md` and published as tenant knowledge (`weekly-skills-refresh-prompt`).
